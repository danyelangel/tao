/* tao main */

// Base function.
var tao = function() {
  // Add functionality here.
  return true;
};


// Version.
tao.VERSION = '0.0.0';


// Export to the root, which is probably `window`.
root.tao = tao;
